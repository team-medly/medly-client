export enum Screens {
  Home = "Home",
}

export enum AppState {
  Unknown = "unknown",
}

export enum Model {
  ModelA = "문헌 검색",
  ModelB = "FAQ",
  ModelC = "MedSurgGPT",
  ModelD = "MedBioGPT",
}

export default { Screens, AppState };
