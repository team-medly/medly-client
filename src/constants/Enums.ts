export enum Screens {
  Home = "Home",
}

export enum AppState {
  Unknown = "unknown",
}

export enum Model {
  ModelA = "문헌 검색(일반)",
  ModelB = "문헌 검색(참조)",
  ModelC = "PatientCareGPT",
  ModelD = "MedSurgGPT",
  ModelE = "MedBioGPT",
}

export default { Screens, AppState };
