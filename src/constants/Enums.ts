export enum Screens {
  Home = "Home",
}

export enum AppState {
  Unknown = "unknown",
}

export enum Model {
  ModelA = "문헌 검색",
  ModelB = "FAQ",
  ModelC = "의료 지식 A",
  ModelD = "MedBioGPT",
}

export default { Screens, AppState };
